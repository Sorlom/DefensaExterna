@model IEnumerable<SistemaVentaPrestamo.Models.Prestamo>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.fechaInicio)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.fechaLimite)
        </th>
        <th>
            @Html.DisplayName("ID Derecho Linea")
        </th>
        <th>
            @Html.DisplayName("Chofer")
        </th>
        <th>
            @Html.DisplayName("Encargado")
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.fechaInicio)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.fechaLimite)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DerechoLinea.idDerechoLinea)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Personal.Login)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Personal1.Login)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idPrestamo }) |
            @Html.ActionLink("Details", "Details", new { id=item.idPrestamo }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idPrestamo })
        </td>
    </tr>
}

</table>
