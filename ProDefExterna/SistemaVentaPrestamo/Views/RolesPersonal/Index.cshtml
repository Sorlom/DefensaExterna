@model IEnumerable<SistemaVentaPrestamo.Models.RolesPersonal>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>     
        <th>
            @Html.DisplayNameFor(model => model.Personal.Login)
        </th>
        <th>
            @Html.DisplayName("Rol")
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Descripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        
        <td>
            @Html.DisplayFor(modelItem => item.Personal.Login)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Roles.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Descripcion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idRolPer }) |
            @Html.ActionLink("Details", "Details", new { id=item.idRolPer }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idRolPer })
        </td>
    </tr>
}

</table>
