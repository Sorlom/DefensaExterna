@model IEnumerable<SistemaVentaPrestamo.Models.ChoferDerLin>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DerechoLinea.Descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Personal.Password)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DerechoLinea.Descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Personal.Password)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idChoDer }) |
            @Html.ActionLink("Details", "Details", new { id=item.idChoDer }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idChoDer })
        </td>
    </tr>
}

</table>
